import React from 'react';

export default class CheckoutComponent extends React.PureComponent {
    render() {
        return (
            <section>
                Checkout
            </section>
        );
    }
}
